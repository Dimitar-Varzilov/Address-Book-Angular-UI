export class Address {
  addressId?: number;
  firstName = '';
  lastName = '';
  telephone: string = '';
  postalCode: number = 0;
  city: string = '';
  country: string = '';
  userAddress: string = '';
  // fullName: string = this.firstName + ' ' + this.lastName;
}
